{
  "skill_name": "neo-code-review",
  "evals": [
    {
      "id": 1,
      "prompt": "Review this Python code:\n```python\nimport sqlite3\n\ndef get_user_data(user_id):\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    query = f\"SELECT * FROM users WHERE id = '{user_id}'\"\n    cursor.execute(query)\n    return cursor.fetchall()\n```",
      "expected_output": "A concise English review report. It must identify SQL injection caused by user_id and the unreleased database resources, classify SQL injection as a 🔴 Critical Issue, and provide concrete remediation using a parameterized query and resource management.",
      "assertions": [
        "The output contains '🔍 Review Summary', '🔴 Critical Issues', '🟡 Suggestions', and '## Validation'",
        "The report identifies the SQL injection vulnerability and its verifiable impact",
        "The report identifies the database connection or cursor resource leak",
        "The report classifies SQL injection as a delivery-blocking issue",
        "The report provides remediation using a parameterized query and resource management",
        "The report is written in English and contains no unsupported additional findings"
      ]
    },
    {
      "id": 2,
      "prompt": "Review this TypeScript code with no obvious problems and provide recommendations:\n```typescript\nexport interface User {\n  id: string;\n  name: string;\n  email: string;\n}\n\nexport function formatUserGreeting(user: User): string {\n  return `Hello, ${user.name}! Welcome back.`;\n}\n```",
      "expected_output": "A concise English review report. 🔴 Critical Issues must be None, the status must not block delivery, and the 🟢 Strengths section should recognize the clear types and simple function design.",
      "assertions": [
        "The output contains '🔍 Review Summary', '🔴 Critical Issues', '🟢 Strengths', and '## Validation'",
        "The 🔴 Critical Issues section explicitly says None",
        "The report does not invent security, performance, or test defects",
        "The report recognizes clear type definitions, simple logic, and good readability"
      ]
    },
    {
      "id": 3,
      "prompt": "An AI Agent just finished modifying code, tests, and configuration. Before handoff, run neo-code-review against the current uncommitted working tree, including staged, unstaged, and untracked files. Stop delivery if critical issues are found.",
      "expected_output": "A concise English report scoped to the current uncommitted working tree. It must cite actual file and line evidence and use 🔴 Critical Issues to decide whether delivery is blocked. If no readable changes exist, it must state that no reviewable changes were found without inventing findings.",
      "assertions": [
        "The report confirms that the current uncommitted working tree was reviewed",
        "The report distinguishes the staged, unstaged, and untracked file inputs",
        "The report cites file paths and line numbers or clearly explains why they are unavailable",
        "If 🔴 Critical Issues are found, the status blocks delivery",
        "The report invents no findings when no changes exist"
      ]
    }
  ]
}
